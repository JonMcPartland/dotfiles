#!/usr/bin/env bash

keygen() {
  EMAIL="$1"
  NAME="$2"

  ssh-keygen -q -t rsa -b 4096 -C $EMAIL -f ~/.ssh/id_rsa_$NAME;
  chmod 0600 ~/.ssh/id_rsa_$NAME.pub;
  chmod 0600 ~/.ssh/id_rsa_$NAME;
  ssh-add ~/.ssh/id_rsa_$NAME.pub;
}

